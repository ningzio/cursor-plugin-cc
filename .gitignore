# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Plugin runtime artifacts (generated when used inside a Claude Code session
# whose CWD happens to be this repo). The actual plugin runtime data lives
# under $CLAUDE_PLUGIN_DATA, but local dev / testing can land state here.
.cursor/
state.json
*.lock

# Test fixtures
tests/.tmp/
